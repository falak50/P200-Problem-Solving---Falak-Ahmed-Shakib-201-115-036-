#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define fastread() (ios_base:: sync_with_stdio(false),cin.tie(NULL))
typedef long long ll;
typedef unsigned long long ull;
typedef pair<ll,ll>pll;
typedef pair<pair<ll,ll>,ll>plll;
#define ff first
#define ss second
#define pb push_back
#define eb emplace_back
const int N=1e6;
int main()
{
    fastread();



    ll t;

    cin>>t;

    while(t--)
    {
        ll ara[5];

        ll a,b,c,d;

        cin>>a>>b>>c>>d;

        ara[0]=a;
        ara[1]=b;
        ara[2]=c;
        ara[3]=d;

        sort(ara,ara+4);

        ll aa=max(a,b);
        ll bb=max(c,d);

         ll f=max(aa,bb);

         ll f2=min(aa,bb);

       // cout<<f<<" "<<f2<<endl;
         if(ara[3]==f and ara[2]==f2)cout<<"YES"<<endl;
         else cout<<"NO"<<endl;




    }




}
